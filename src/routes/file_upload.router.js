import { Router } from 'express';
const router = Router();

// 프로필 사진 업로드
router.post('/api/users/:userId/profile-image', async (req, res) => {
 
});

// 게시물 이미지 업로드
router.post('/api/users/:userId/post-image', async (req, res) => {
    
});

export default router;
